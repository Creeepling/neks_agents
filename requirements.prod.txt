# Production requirements — extends requirements.txt with Postgres drivers.
# Install with: pip install -r requirements.txt -r requirements.prod.txt
#
# Used in the Dockerfile for Cloud Run deployment.

# File processing and PDF generation
python-pptx
svglib
reportlab

# PostgreSQL sync driver (used by SQLAlchemy for Cloud SQL / Neon / Supabase)
psycopg2-binary==2.9.9

# LLM
google-genai>=1.16.0
instructor>=1.7.0
jsonref

# PostgreSQL async driver (optional — needed only if you add async SQLAlchemy)
asyncpg==0.29.0

# Alembic for database migrations
alembic==1.13.3
